extends layout

block content
  h1=title

  ul
    each location in locationList
      li 
        a(href=location.url) #{location.facilityName} #{location.unitNum} #{location.roomNum}

    else
      li Currently we don't have any locations in the database